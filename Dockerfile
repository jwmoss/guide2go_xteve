FROM alpine:latest

RUN apk add --no-cache \
    ca-certificates \
    less \
    ncurses-terminfo-base \
    krb5-libs \
    libgcc \
    libintl \
    libssl1.1 \
    libstdc++ \
    tzdata \
    userspace-rcu \
    zlib \
    icu-libs \
    curl \
    ffmpeg \
    vlc

# Volumes
VOLUME /config
VOLUME /root/.xteve
VOLUME /tmp/xteve
